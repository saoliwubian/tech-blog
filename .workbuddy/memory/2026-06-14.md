# 2026-06-14 工作日志

## TechPulse 科技日报自动化执行
- 完成第 005 期科技日报生成 (`public/ai-daily/2026-06-14.html`)
- 头条: 美国政府下令 Anthropic 禁用 Fable 5 / Mythos 5 模型
- 6 条重点分析 + 4 条速览 + 2 张 Mermaid 图表
- 新闻来源: TechCrunch, The Verge, WSJ, Fortune, Bloomberg, NVIDIA Blog, Wired, Tom's Hardware, 新浪财经, Wasmer Blog 等
- Git commit c439e3c, pushed to main (先 merge 后 push，解决 2026-06-13.html 冲突)
- 更新 index.html，新增 06-14 条目并移除 06-13 的 NEW 标签

## 修复日期排版与重复条目
- 问题: index.html 因 merge 产生大量重复条目 (19条→9条)，多个日期仍带 NEW 标签
- 日期格式: 06-14.html 使用 `月 日` (有空格)，统一为 `06月14日` (无空格)
- Git commit 3d425c7, pushed to main

## 修复 Astro Check 类型错误
- `LightDarkSwitch.svelte`: Svelte 5 无显式 props 导致 `client:only` 指令类型错误，添加 `let {} = $props()` 解决
- `ArchivePanel.svelte`: `Post` 接口 `category?: string` 与 `PostForList.category: string | null` 不兼容，改为 `category?: string | null`；`tags`/`categories` 无默认值报错，添加 `= []`
- `astro check` 0 error, 0 warning
- Git commit e8c5b04, pushed to main
